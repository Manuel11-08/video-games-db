/**
 * @file
 * Video Games Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.videoGamesTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
